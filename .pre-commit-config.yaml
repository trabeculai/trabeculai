repos:
  - repo: local
    hooks:
      - id: quality
        name: TrabeculAI quality checks
        entry: uv run --locked python scripts/quality.py
        language: system
        pass_filenames: false
        always_run: true
        stages:
          - pre-commit
